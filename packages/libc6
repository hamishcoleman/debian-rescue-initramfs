#!/bin/bash

DEBOOT="$1"
ARCH="$2"
STAGE="$3"

DEL2="
    $DEBOOT/usr/lib/$ARCH-linux-gnu/gconv/*
"


if [ "$STAGE" = "minimise" ]; then
    # remove files
    rm -f $DEL2/$i
else
    echo ERROR: unknown stage
    exit 1
fi

