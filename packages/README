This directory contains scripts named after debian packages.  Each script is
responsible for the customisations that need to be applied to that package.

These customisations are applied in several phases, and each package that is
found installed in the rescue system will attempt to run its matching script
during each phase.

Curently the phases are:

"minimise"
    During this phase, all un-needed files are deleted.

