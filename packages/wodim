#!/bin/bash

DEBOOT="$1"
STAGE="$2"

DEL="
    /etc/netscsid.conf
    /usr/bin/readom
    /usr/sbin/netscsid
"


if [ "$STAGE" = "minimise" ]; then
    # remove files
    for i in $DEL; do
        rm -f $DEBOOT/$i
    done
else
    echo ERROR: unknown stage
    exit 1
fi

