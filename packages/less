#!/bin/bash

DEBOOT="$1"
ARCH="$2"
STAGE="$3"


if [ "$STAGE" = "minimise" ]; then

    # overwrite the system more with this one
    ln -sf /bin/less ${DEBOOT}/bin/more

else
    # unsupported stage
    exit 1
fi

