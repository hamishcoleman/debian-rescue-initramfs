#!/bin/bash

DEBOOT="$1"
ARCH="$2"
STAGE="$3"

DEL2="
    $DEBOOT/usr/lib/$ARCH-linux-gnu/libicu*.so.52
    $DEBOOT/usr/lib/$ARCH-linux-gnu/libicu*.so.52.1
"


if [ "$STAGE" = "minimise" ]; then
    # remove files
    rm -f $DEL2
else
    echo ERROR: unknown stage
    exit 1
fi

